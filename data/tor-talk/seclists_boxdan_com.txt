
@_date: 2019-03-20 15:16:17
@_author: Frank Beuth 
@_subject: [tor-talk] tor running as root on macOS by default 
After installing the command line tor executable via the macOS Homebrew package manager as documented here:
and having it start via macOS launchd using the `brew services` manager
I see that `tor` is running as root.
This seems bad.
(the process was started by launchd so the homebrew package is definitely at It's not clear where to file a bug report (the Tor bug tracker? the Homebrew github?) so hopefully someone on this list can route the issue properly.
